import React from 'react'

import Start from '../Start/Start'
import styles from './Main.scss'

const Main = () =>
  <div>
    <h1 className={styles.header}>Control</h1>
    <Start />
  </div>

export default Main
